<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TatamiArena.App.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TatamiArena.App.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel Spacing="16">
            <TextBlock Text="Welcome to Tatami Arena" 
                     Style="{StaticResource TitleLargeTextBlockStyle}"/>
            <TextBlock Text="Your Karate Tournament Management System" 
                     Style="{StaticResource SubtitleTextBlockStyle}"/>
            
            <Grid Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" 
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          Padding="16"
                          CornerRadius="8">
                    <TextBlock Text="Competitions" 
                             Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Name="CompetitionsCount" 
                             Text="0"
                             Style="{StaticResource DisplayLargeTextBlockStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" 
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          Padding="16"
                          CornerRadius="8"
                          Margin="16,0">
                    <TextBlock Text="Categories" 
                             Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Name="CategoriesCount" 
                             Text="0"
                             Style="{StaticResource DisplayLargeTextBlockStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" 
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          Padding="16"
                          CornerRadius="8">
                    <TextBlock Text="Competitors" 
                             Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBlock x:Name="CompetitorsCount" 
                             Text="0"
                             Style="{StaticResource DisplayLargeTextBlockStyle}"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page> 